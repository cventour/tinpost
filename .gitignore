node_modules/
*.log

# A local data directory, if anyone points --data-dir at the repo
data/
*.db
*.db-wal
*.db-shm
blobs/

# Logo working files: the supplied sheet and every crop and resize taken from it.
# Kept on disk so the marks can be regenerated, never pushed. What the project
# actually needs is committed: the lockups in docs/brand/ that the README shows,
# and the icons in src/web/public/ that the server reads at startup.
brand-source/

# The design skill's machine-readable sidecar. Nothing reads it, and it describes the
# UI as it stood at v0.2.0 — DESIGN.md is the current, maintained account.
.impeccable/

# npm pack output
*.tgz
